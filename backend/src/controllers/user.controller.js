const asyncHandler=require("../utils/asyncHandler");const db=require("../repositories/postgres.repository");const authService=require("../services/auth.service");
const getUsers=asyncHandler(async(req,res)=>{const sql=req.user.role==="admin"?"SELECT * FROM users ORDER BY name":"SELECT DISTINCT u.* FROM users u JOIN project_members target ON target.user_id=u.id WHERE EXISTS (SELECT 1 FROM project_members mine WHERE mine.project_id=target.project_id AND mine.user_id=$1) ORDER BY u.name";const users=(await db.query(sql,req.user.role==="admin"?[]:[req.user.userId])).rows.map(db.toUser);res.json({success:true,count:users.length,data:users});});const createUser=asyncHandler(async(req,res)=>{const user=await authService.register(req.body);res.status(201).json({success:true,message:"User created successfully",data:user});});module.exports={getUsers,createUser};
